@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using SqliteDemo.Models.Entity;
@model Events[]
<h4>Event List</h4>
<ul>
    @foreach (Events events in Model)
    {
        <li>@events.eventId,  @events.userId,  @events.EventName</li>
    }
</ul>